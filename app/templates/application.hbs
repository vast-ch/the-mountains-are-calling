{{page-title (t "project.name")}}

<div class="container mx-auto px-4 sm:px-6 lg:px-8 h-full flex flex-col">
  <Menu />

  <Map
    @startDate={{this.startDate}}
    @endDate={{this.endDate}}
    @deviceId={{this.deviceId}}
    @onChange={{this.onChange}}
  />
</div>